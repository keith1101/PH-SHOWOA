# VRPenstein (Python)

Python port of the VRPenstein C++ implementation.

## Run

From this folder:

```
python -m vrpenstein --problem path\to\instance.txt [options]
```

## Example

```
python -m vrpenstein --problem ..\data\Liu_Tang_Yao\example.txt --runs 1 --pop_size 64
```
